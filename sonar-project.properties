# ==== Project Identification ====
sonar.projectKey=mounika110907_Quiz_Spark
sonar.organization=mounika110907
sonar.projectName=Quiz_Spark
sonar.projectVersion=1.0

# ==== Source Files ====
# Include backend (Python files) and optionally static/templates if they contain code to analyze.
# Usually, static/templates don't have analyzable Python code, so focus on backend.
sonar.sources=backend

# ==== Encoding ====
sonar.sourceEncoding=UTF-8

# ==== Optional: Host URL (for clarity, but SonarCloud already knows this) ====
sonar.host.url=https://sonarcloud.io

# ==== Authentication ====
# You should NOT include sonar.login in the file for security reasons.
# GitHub Actions already passes SONAR_TOKEN via environment variable.
# So remove this line ↓
# sonar.login=${SONAR_TOKEN}

# ==== (Optional) Ignore folders ====
sonar.exclusions=**/__pycache__/**, **/venv/**, **/tests/**

# ==== (Optional) Python Coverage Support (if you run pytest with coverage) ====
# sonar.python.coverage.reportPaths=coverage.xml
